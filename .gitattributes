# Normalize line endings to LF in the repo, checkout as-is on Windows
* text=auto eol=lf

# Binaries — no conversion
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.woff2 binary
*.pdf binary
*.zip binary
